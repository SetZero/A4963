SET(LIBNAME inc)

file(GLOB SOURCE_FILES *.c)
file(GLOB HEADER_FILES *.h)

SET_SOURCE_FILES_PROPERTIES( ${SOURCE_FILES} ${HEADER_FILES} PROPERTIES LANGUAGE CXX )
add_library(${LIBNAME} STATIC ${SOURCE_FILES} ${HEADER_FILES})